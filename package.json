{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch --silent --verbose --colors --noStackTrace",
    "test:staged": "jest --passWithNoTests --findRelatedTests --silent --noStackTrace",
    "test:ci": "jest --covarage"
  },
  "standard": {
    "env": ["jest"]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^4.3.8",
    "jest": "^27.1.0",
    "jest-cli": "^27.1.0",
    "lint-staged": "^11.1.2",
    "standard": "^16.0.3"
  }
}
